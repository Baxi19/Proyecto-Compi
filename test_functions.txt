let a = "Functions";

// method to plus 1
let plus1 = fn(y){
	return (y+1);
}

// Sub method
let sub = fn(e,r){
	return (e-r);
}


// iter list to print data recursive
let map = fn(n){
    	if (len(n) == 1){
        	return first(n);
	}else{
		puts(first(n));
        	return map(rest(n));
    }
}

// Main App
let main = fn(a){
	let b = 5;
	puts(plus1(b));

	puts(sub(100,50));

	let list = [1,2,3,4,5];
	puts(map(list));
}